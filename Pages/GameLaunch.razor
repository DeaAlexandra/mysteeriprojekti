@page "/"
@inject NavigationManager Navigation
@inject RandomizationService RandomizationService

<div class="launch-container">
    <h1>Mysteeriprojekti</h1>
    <h2>Aloita peli</h2>
    <button @onclick="StartGame">
        <img src="6188818.png" alt="Start Game" class="button-image" />
    </button>
</div>

<style>
    .launch-container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    .launch-container h1 {
        margin-bottom: 20px;
    }

    .launch-container h2 {
        margin-bottom: 20px;
    }

    .launch-container button {
        padding: 10px 20px;
        font-size: 16px;
        border: none;
        background: none;
        cursor: pointer;
    }

    .button-image {
        width: 50px; /* Adjust the size as needed */
        height: 50px; /* Adjust the size as needed */
    }
</style>

@code {
    private void StartGame()
    {
        /*
        RandomizationService.GenerateRandomCharacters();
        RandomizationService.GenerateRandomWeapons();
        */
        Navigation.NavigateTo("/home");
    }
}